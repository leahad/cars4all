<form method="post" action="{{ path('delete', {'id': car.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ car.id) }}">
	<button type="submit" class="btn btn-primary mt-4">Delete</button>
</form>
